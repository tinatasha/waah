import { CountPipePipe } from './count-pipe.pipe';

describe('CountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
